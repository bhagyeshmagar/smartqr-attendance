# ===========================================
# SmartQR Attendance - Production Environment
# ===========================================
# Copy this file and fill in the values for production deployment

# Database (Neon PostgreSQL - Free Tier)
# Sign up at https://neon.tech and create a project
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=postgresql://your-user:your-password@your-host.neon.tech/smartqr?sslmode=require

# Redis (Upstash - Free Tier)
# Sign up at https://upstash.com and create a Redis database
# Format: rediss://default:password@host:port
REDIS_URL=rediss://default:your-password@your-host.upstash.io:6379

# JWT Authentication (generate secure random strings)
# Use: openssl rand -base64 32
JWT_SECRET=generate-a-secure-random-string-here
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_SECRET=generate-another-secure-random-string-here
REFRESH_TOKEN_EXPIRES_IN=7d

# HMAC Secret for QR Token Signing (generate secure random string)
HMAC_SECRET=generate-hmac-secret-here

# QR Token Configuration
QR_TTL_SECONDS=30

# Server Configuration
NODE_ENV=production
PORT=4000

# CORS Origins (your frontend URL from Render)
# Example: https://smartqr-frontend.onrender.com
CORS_ORIGINS=https://your-frontend-url.onrender.com

# Rate Limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100
SCAN_RATE_LIMIT_TTL=60
SCAN_RATE_LIMIT_MAX=10

# Frontend Environment Variables (set in Render dashboard)
# VITE_API_URL=https://smartqr-backend.onrender.com
# VITE_WS_URL=https://smartqr-backend.onrender.com
